{
  "name": "Ubuntu 22.04 - C/C++",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  // Port mappings for z21 UDP services
  "runArgs": [
    "--cap-add=SYS_PTRACE",
    "--security-opt=seccomp=unconfined",
    "-p", "21105:21105/udp",
    "-p", "21106:21106/udp"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools"
      ]
    }
  },
  "postCreateCommand": "ldconfig || true && mkdir -p /www/config",
  // Bind mount host config folder into container
  "mounts": [
    "source=/Users/ralf/Documents/github/backup_config/,target=/www/config/,type=bind,consistency=cached"
  ]
}
